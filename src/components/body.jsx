import React, { Component } from 'react';

// const electron = window.require('electron');

class Body extends Component {
  render() {
    return (
      <div id='card' />
    );
  }
}

export default Body;
